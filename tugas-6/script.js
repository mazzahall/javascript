// latihan 1
let dressSize = 45;
dressSize = String(dressSize);
console.log(dressSize);
dressSize = +dressSize;
console.log(dressSize);

// latihan 2
let isLight = true;
isLight = String(isLight);
console.log(isLight);